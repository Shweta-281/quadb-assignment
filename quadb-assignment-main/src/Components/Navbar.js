import "../Stylesheets/Navbar.css"

const Navbar = ({handleChange}) => {
    return (
            <nav className="nav-main">
                <h2 className="nav-header">Ticket Booking <span className="nav-h">App</span></h2>
            </nav>
    )
}

export default Navbar